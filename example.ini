[camera]
name = Camera name
enabled = Take photos?
exposure = milliseconds

[ftp]
enabled = whether the uploader is on
replace = upload single image and replace
resize = resize images when upload and replace?
timestamped = upload a timestamped image
server = your.domain.or.ip
directory = /the/dir/your/timelapses/live/in
user = ftp username
pass = ftp password

[timelapse]
interval = seconds
starttime = format: HH:MM
stoptime = format: HH:MM

[localfiles]
spooling_dir = writable dir
upload_dir = writable dir

[picam_size]
width = 640
height = 480

[eosserialnumber]
value = 0

